# ─── Core ───
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.3
pydantic-settings==2.9.1

# ─── Database ───
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
alembic==1.15.2
greenlet==3.2.2

# ─── Background tasks (v1.0.0g — outbound webhook delivery retries + log prune) ───
# AsyncIOScheduler runs jobs in the same event loop as FastAPI, so they share
# the async SQLAlchemy session machinery without thread-safety concerns.
apscheduler==3.10.4

# ─── HTTP client (v1.0.0g — outbound webhook delivery) ───
# httpx is already pulled in transitively by FastAPI's TestClient, but we
# pin it explicitly because the webhook service is a production consumer.
httpx==0.28.1

# ─── Auth ───
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
python-multipart==0.0.20

# ─── Logging ───
structlog==25.4.0

# ─── Testing ───
pytest==8.3.5
pytest-asyncio==0.26.0

# ─── Utilities ───
python-dotenv==1.1.0
email-validator==2.2.0

# ─── PDF generation (v27) ───
fpdf2==2.8.2
